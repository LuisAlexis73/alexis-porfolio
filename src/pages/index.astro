---
import MainLayout from "@/layouts/MainLayout.astro";

import LinkButton from "@/components/LinkButton.astro";
import MailIcon from "@/assets/icons/MailIcon.astro";
import ProjectCard from "@/components/ProjectCard.astro";
import BriefcaseIcon from "@/assets/icons/BriefcaseIcon.astro";
import Badge from "@/components/Badge.astro";
import { getCollection } from "astro:content";

const projects = await getCollection(
  "projects",
  (project) => project.data.featured
);
---

<MainLayout title="Inicio | Alexis">
  <section class="max-w-5xl mx-auto">
    <header class="mb-8 flex gap-4 justify-center items-center">
      <h1 class="text-3xl lg:text-5xl font-bold text-center">
        Hola y Bienvenid@
      </h1>
      <Badge>Disponible Para Trabajar</Badge>
    </header>

    <div class="my-8">
      <p class="text-neutral-300 text-[18px] prose max-w-5xl">
        <span class="text-yellow-400/90">Mi nombre es Alexis y soy desarrollador web con 2 años de
          experiencia</span> trabajando para un StartUp tecnológico. <br /> A lo largo de
        mi trayectoria, he trabajado con tecnologías como<span class="text-yellow-400/90"> JavaScript, TypeScript,
        NestJS, Docker, PostgreSQL, MySQL y React</span>. Además, cuento con conocimientos
        en <span class="text-yellow-400/90">HTML, CSS, Bootstrap, TailwindCSS, Git/GitHub, Astro y metodologías ágiles
        como Scrum</span>. También tengo experiencia en el uso de herramientas de pruebas
        como<span class="text-yellow-400/90"> Jest y Cypress</span>; simepre buscando aprender y adaptarme a nuevas tecnologías
        implementando buenas prácticas.
      </p>
    </div>

    <div class="flex gap-3 my-4">
      <LinkButton href="/projects">Proyectos <BriefcaseIcon /> </LinkButton>
      <LinkButton href="mailto:la.galarza@outlook.com"
        >Contáctame <MailIcon />
      </LinkButton>
    </div>

    <h2 class="text-2xl font-semibold my-6">Proyectos destacados</h2>
    <ul class="grid lg:grid-cols-2 gap-4 items-stretch">
      {
        projects.map((p) => (
          <li>
            <ProjectCard project={p} />
          </li>
        ))
      }
    </ul>
  </section>
</MainLayout>
